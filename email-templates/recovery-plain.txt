Reset your Relay password

Open this link to choose a new password for {{ .Email }}:
{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=recovery

If you did not request this reset, ignore this message and notify the project owner if you suspect misuse.
